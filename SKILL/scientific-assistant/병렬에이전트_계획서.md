# Plan: 병렬 멀티에이전트 오케스트레이션

## Context
현재 app.py는 스킬 여러 개를 하나의 프롬프트에 합쳐서 단일 LLM 호출.
실제 병렬 에이전트 실행이 안 됨. API/GGUF 분리해서 병렬 실행 필요.

## 핵심 규칙
- **API는 API만** (최대 4 에이전트, ThreadPoolExecutor 병렬)
- **GGUF는 GGUF만** (최대 2 에이전트, 순차 직렬)
- **혼합 금지**
- 스킬 1개 또는 같은 그룹 → 기존 방식 유지
- 각 스킬은 독립 (로그프레소 ≠ 도메인 지식 ≠ 코드)
- 특수 스킬(logpresso-search, knowledge-search) 선처리 후 나머지만 병렬

## 수정 파일
`/home/user/ASAS/SKILL/scientific-assistant/app.py`

## 구현 (8개 항목)
1. import 추가
2. SKILL_GROUPS + group_skills_for_parallel()
3. _agent_call() — API용
4. _agent_call_gguf() — GGUF용
5. _synthesize_responses() — API 합성
6. _synthesize_responses_gguf() — GGUF 합성
7. /api/chat 분기 (API 병렬 / GGUF 순차 / 기존 폴백)
8. 프론트엔드 [🔀 병렬 N에이전트] 표시

## 검증
- 스킬 1개 → 기존 동일
- API + 다른 그룹 2개+ → 병렬 → 합성
- GGUF + 다른 그룹 2개 → 순차 → 합성
- 실패 → 기존 폴백
