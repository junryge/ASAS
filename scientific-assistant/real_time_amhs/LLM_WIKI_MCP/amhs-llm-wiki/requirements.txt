# 필수
flask>=3.0

# 선택: PDF 텍스트 추출 (없으면 PDF는 원본 보관만 됨)
pypdf>=4.0

# 선택: 벡터 연산 가속 (없으면 순수 파이썬으로 동작)
numpy>=1.24

# 선택: 로컬 임베딩/리랭커를 쓸 때만 (API 백엔드면 불필요)
# sentence-transformers>=3.0

# 선택: MCP 서버(mcp_server.py) 실행 시
# mcp>=1.27,<2
