---
title: 영역분리 — FAB 파일에서 area_score 가 자리를 옮긴다
type: concept
domain: 관제
tags: [영역분리, area_score, all_score, 정규화, FAB파일, jupyter_csv, hot_area, all_hot_area]
summary: FAB 파일에는 전체 점수와 그 FAB 점수가 같이 들어 있다. 관제가 받는 순간 area_score 를 unified_risk_score 자리로 옮기고 원본은 all_* 로 남긴다.
sources: []
author: 
updated: 
---

## 왜 자리를 바꾸나

FAB 파일에는 **전체 점수와 그 FAB 점수가 같이** 들어 있다. 관제가 그대로 읽으면 M14 화면이 전체 점수로 등급을 매기고 케이스 영역이 M16HUB 로 찍힌다 — 화면 전체가 남의 데이터를 보게 된다.

| 받은 그대로 | 정규화 후 | 값 |
|---|---|---|
| `unified_risk_score` (전체) | `all_score` | 137 |
| `area_score` (M14) | **`unified_risk_score`** | 28 |
| `unified_risk_level` (전체) | `all_level` | 주의 |
| `area_level` (M14) | **`unified_risk_level`** | 관심 |
| `hot_area` (전체 기준) | `all_hot_area` | M16HUB |
| — | **`hot_area`** | M14 |

**한 자리에서 한 번만 바꾼다.** 그래프·예보·기여도·리포트·정확도가 모두 `unified_risk_score` 와 `hot_area` 를 읽으므로, 받는 자리에서 바꿔 두면 하위 모듈을 하나도 안 고치고 FAB 화면이 자기 데이터를 본다. 원본은 `all_*` 로 남겨 전체와 비교할 수 있게 둔다.
