'⬢ 아이소메트리' — three.js 3D 보기. 덮어쓸 파일 목록
════════════════════════════════════════════════════════════════════

월드모델파생/
├── main.py                         바꿈  (/static 서빙 한 덩이)
├── oht_world.spec                  바꿈  (datas 에 static 추가 — exe 로 묶을 때)
├── dashboard.html                  바꿈  (단추 · #map-3d · '3D 아이소메트리' 절)
├── static/js/oht3d/
│   ├── three.module.min.js         ★새 파일 (세트 그대로, r169)
│   ├── oht3d.js                    ★새 파일 (세트 + 아래 손본 것)
│   └── README.md                   ★새 파일 (무엇을 왜 손봤나)
└── tests/
    ├── test_iso3d.py               ★새 파일
    └── iso3d.js                    ★새 파일

무엇이 보이나
────────────────────────────────────────────────────────────────────
  보기 단추가 셋: [▭ 2D] [◈ 유사 3D] [⬢ 아이소메트리]. 기본은 그대로 2D.
  아이소메트리를 누르면 같은 자리에 three.js 로 레일·행거·설비·차량이 서고,
  등각(45°·35.264°) 카메라로 연다. 방향은 2D·유사 3D 와 같다 (x 는 오른쪽 아래,
  2D 의 위쪽이 화면 오른쪽 위).
    · 드래그 회전 · 휠 줌 · 우클릭(또는 Shift+드래그) 이동 · 더블클릭 = 정체 지점
    · 차량 클릭 = 추적(따라가기) · 존 클릭 = 그 HID Zone 패널
    · 위 바: 정체 지점 · 전체 · 아이소로/원근으로 · ID·속도 · 히트맵 · 차량 크기 · 이미지 저장 · 패널
    · 오른쪽 OHT 상태 / HID Zone 목록의 더블클릭도 3D 카메라가 간다. ⤢ 전체보기도.
  차량 색은 ⚙ 설정의 그 색(공차·적재·정지·JAM·OBS) 이고, 테마(HMI/다크)도 따라간다.
  벽은 없다 (고객: "벽 필요없다").

  위 '표시' 토글이 3D 에도 그대로 먹는다
    Zone(존 바닥·이름) · 차단(빨간 레일) · ID(차량 라벨) · 주소(노드 번호, 가까이서) ·
    포트(설비 상자) · 라벨(ZC/HID/베이/열/MTL) · 합류(✕ ○) · 센서(●) · 체인(핫스팟 링)
  ⚙ 설정에 '3D 아이소메트리' 묶음 — 차량 크기 · 설비(포트) 크기 · 글자 크기 · 레일 굵기 (배수).
    저장하면 이 브라우저에 남는다 (2D 설정과 같은 자리).

데이터는 2D 와 같다
────────────────────────────────────────────────────────────────────
  레이아웃 = /api/layout-graph (그대로), 차량 = 재생 프레임(updateUI) 그대로.
  3D 만의 조회 길은 없다. 재생 엔진·점수·2D 그리기는 안 건드렸다.

넣을 때 확인
────────────────────────────────────────────────────────────────────
  ① static/ 폴더가 main.py 와 **같은 폴더**에 있어야 한다 (bundled_dir 기준).
     없으면 서버 콘솔에 "[경고] static 폴더가 없다" 가 찍히고, 단추를 누르면
     화면에 못 받은 이유가 뜬다.
  ② 서버를 다시 띄워야 한다 (main.py 가 바뀌었다).
  ③ exe 로 묶는다면 oht_world.spec 그대로 다시 build.bat.
  ④ 브라우저는 http://localhost:10005 로 — file:// 로 열면 ES 모듈을 막는다.

확인한 것
────────────────────────────────────────────────────────────────────
  · 실물 M14A 레이아웃(9,403 노드 · 10,424 엣지 · HID 존 187 · 설비 1,240) 을
    브라우저에 띄워 등각/원근/다크/존 포커스/차량 추적/2D 복귀를 찍어 봤다.
  · 시험: tests/test_iso3d.py (35) + tests/test_layout_parse.py (13) 전부 OK
      python3 -m unittest tests.test_iso3d tests.test_layout_parse

차량이 "너무 빠르다" 에 대해
────────────────────────────────────────────────────────────────────
  원래 빠른 게 맞다. 재생 엔진이 x1 에서 **데이터 2초짜리 프레임을 0.1초마다** 보낸다
  (main.py 의 WS 루프 — timeout 0.1 초마다 advance_frame 1회; 프레임은 data_loader 가
  2초 단위로 묶는다 → 실시간의 20배. x5 는 100배).
  2D 는 점이 2 m 넘게 뛰면 순간이동시켜서(interpolateLoop 의 200 단위 규칙) 빠른 게
  티가 안 났고, 3D 는 레일을 따라 미끄러지니 그대로 보인다. 3D 가 더 빠른 게 아니라
  같은 속도가 **보이는** 것이다.
  진짜로 x1 = 실시간이 되게 하려면 WS 루프가 프레임 간격(2초)÷배율 만큼 기다리게
  바꿔야 하는데, 그러면 2D 재생도 같이 느려진다 — 그렇게 할지는 결정이 필요하다.
